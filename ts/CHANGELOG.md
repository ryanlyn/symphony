# Changelog

## 2026-05-06

- Packaged the TypeScript port with a `symphony-ts` CLI, Ink terminal TUI, HTTP observability API,
  `symphony-ts runs`, rotating runtime log setup, and npm package dry-run coverage.
- Added live TS canaries for local Codex app-server, Codex resume, Linear plus Codex, and Claude
  stream-json smoke coverage.
- Added shared parity fixtures for prompt rendering, terminal dashboard formatting, workflow file
  drift, and Elixir-shaped observability payloads.
- Copied `WORKFLOW.md`, `WORKFLOW_FULL_ACCESS.md`, `WORKFLOW_ENSEMBLE.md`, and
  `WORKFLOW_ALPHA_EVOLVE.md` from Elixir as byte-identical package inputs.

## Parity Scope

The Elixir historical changelog remains in `../elixir/CHANGELOG.md`. This changelog records the
TypeScript package surface and parity milestones. Source-level and commit-derived parity evidence is
tracked under `../docs/parity/`.
